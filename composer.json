{
    "require": {
        "dompdf/dompdf": "^3.1"
    },
    "require-dev": {
        "phpunit/phpunit": "^10.5"
    },
    "autoload-dev": {
        "classmap": ["tests/"]
    },
    "scripts": {
        "test": "vendor/bin/phpunit",
        "test-verbose": "vendor/bin/phpunit --testdox"
    }
}
